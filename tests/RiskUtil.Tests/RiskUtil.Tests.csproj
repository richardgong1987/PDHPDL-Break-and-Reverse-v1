<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.8.0" />
    <PackageReference Include="xunit" Version="2.6.6" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.5.6" />
  </ItemGroup>

  <!-- Link the pure domain class directly instead of referencing the cBot project,
       so tests stay free of the cTrader.Automate / cAlgo.API dependency. -->
  <ItemGroup>
    <Compile Include="..\..\PDHPDL Break and Reverse v1\Util\RiskUtil.cs" Link="RiskUtil.cs" />
  </ItemGroup>

</Project>
